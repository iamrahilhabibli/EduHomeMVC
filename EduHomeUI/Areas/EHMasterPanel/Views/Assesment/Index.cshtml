@using EduHomeUI.Areas.EHMasterPanel.ViewModels.AssesmentViewModels;
@model AssesmentIndexViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
<partial name="_Notification" />
@{
    int count = 1;
}
<div class="container-fluid">
    <div class="row">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Assesments</h4>
                <a asp-controller="Assesment" asp-action="Create" class="btn btn-primary mb-4">Create New Assesment</a>
                <div class="table-responsive">
                    <table class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Assesment</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var assesment in Model.Assesments)
                            {
                                <tr>
                                    <td>@count</td>
                                    <td>@assesment.AssesmentType</td>
                                    <td>
                                        <a asp-action="Delete" asp-route-id="@assesment.Id" class="btn btn-danger"><i class="bi bi-trash-fill"></i></a>
                                    </td>
                                </tr>
                                count++;
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
