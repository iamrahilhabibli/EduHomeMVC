@using EduHomeUI.Areas.EHMasterPanel.ViewModels.SettingViewModels;
@model SettingIndexViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
<partial name="_Notification" />
@{
    ViewData["Title"] = "Index";
    int count = 1;
}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Settings</h4>
                    <a asp-controller="Setting" asp-action="Create" class="btn btn-primary mb-4">Create New Setting</a>
                    <div class="table-responsive">
                        <table class="table table-striped table-bordered table-hover" id="dataTableData">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Key</th>
                                    <th>Value</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var setting in Model.Settings)
                                {
                                    @if (!setting.IsDeleted)
                                    {
                                        <tr>
                                            <td>@count</td>
                                            <td>@setting.Key</td>
                                            <td>@setting.Value</td>
                                            <td>
                                                <a asp-action="Update" asp-route-id="@setting.Id" class="btn btn-warning"><i class="bi bi-pencil-square"></i></a>
                                                <a asp-action="Delete" asp-route-id="@setting.Id" class="btn btn-danger"><i class="bi bi-trash-fill"></i></a>

                                            </td>
                                        </tr>
                                        count++;
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts{
    <script src="~/datatable_file.js"></script>
}