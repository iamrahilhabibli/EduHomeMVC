@using EduHomeUI.Areas.EHMasterPanel.ViewModels.TeacherViewModels;
@model TeacherIndexViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
<partial name="_Notification" />
@{
    ViewData["Title"] = "Index";
    int count = 1;
}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Teachers</h4>
                    <a asp-controller="Teacher" asp-action="Create" class="btn btn-primary mb-4">Create New Teacher</a>
                    <div class="table-responsive">
                        <table class="table table-striped table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Image</th>
                                    <th>Name</th>
                                    <th>Surname</th>
                                    <th>Position</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var teacher in Model.Teachers)
                                {
                                    @if (!teacher.IsDeleted)
                                    {
                                        <tr>
                                            <td>@count</td>
                                            <td><img src="@teacher.ImagePath" style="width:100px" /></td>
                                            <td>@teacher.Name</td>
                                            <td>@teacher.Surname</td>
                                            <td>@teacher.Position</td>
                                            <td>
                                                <a asp-action="Update" asp-route-id="@teacher.Id" class="btn btn-warning"><i class="bi bi-pencil-square"></i></a>
                                                <a asp-action="Delete" asp-route-id="@teacher.Id" class="btn btn-danger"><i class="bi bi-trash-fill"></i></a>
                                                <a asp-controller="Teacher" asp-action="Details" asp-route-id="@teacher.Id" class="btn btn-success"><i class="bi bi-chat-left-dots"></i></a>

                                            </td>
                                        </tr>
                                        count++;
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
