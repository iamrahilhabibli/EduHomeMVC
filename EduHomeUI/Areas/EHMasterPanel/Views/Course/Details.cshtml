@using EduHome.Core.Entities;
@model CourseDetails
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
<partial name="_Notification" />
@{
	ViewData["Title"] = "Index";
	int count = 1;
}
<div class="container-fluid">

	<!-- start page title -->
	<div class="row">
		<div class="card">
			<div class="card-body">
				<h4 class="card-title">Sliders</h4>
@*				<a asp-controller="Course" asp-action="Create" class="btn btn-primary mb-4">Create New Course</a>*@
				<div class="table-responsive">
					<table class="table table-dark mb-0">

						<thead>
							<tr>
								<th>#</th>
								<th>Image</th>
								<th>Name</th>
								<th>Description</th>
								<th>Actions</th>
							</tr>
						</thead>
						<tbody>
							
								<tr>
									<th scope="row">@count</th>
									<td> </td>
									<td>@Model.Duration</td>
									<td>@Model.ClassDuration</td>
@*									<td>
										<a asp-action="Update" asp-route-id="@course.Id" class="btn btn-warning mb-4">	<i class="bi bi-pencil-square"></i></a>
										<a asp-action="Delete" asp-route-id="@course.Id" class="btn btn-danger mb-4"><i class="bi bi-trash-fill"></i></a>
										<a asp-action="Details" asp-route-id="@course.Id" class="btn btn-success mb-4"><i class="bi bi-chat-left-dots"></i></a>
									</td>*@
								</tr>

						</tbody>
					</table>
				</div>

			</div>
		</div>
	</div>
</div>